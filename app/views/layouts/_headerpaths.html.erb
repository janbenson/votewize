   <%= link_to image_tag('/assets/home.png') ,  'sessions/' + (@user.id).to_s + '/Learn', :remote => true, :data => @user.id  %>
   <%= link_to image_tag('/assets/donate.png') ,  'sessions/' + (@user.id).to_s + '/discuss', :remote => true, :data => @user.id  %>
