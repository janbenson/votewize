$("#edit_category").before('<div id="flash_notice"><%= escape_javascript(flash.delete(:notice)) %></div>');
$var z = "<%= escape_javascript @counter %>"
$("#edit_category").html("<%= escape_javascript @category.surveys[z].question, 'Question') %>");
